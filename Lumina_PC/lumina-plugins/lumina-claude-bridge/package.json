{
  "name": "@lumina/lumina-claude-bridge",
  "version": "2026.7.2",
  "description": "Lumina Claude bridge — dispatch prompts to the Claude desktop app / Claude Code CLI and read status back. Adapters for locate/read/write against the Claude App.",
  "type": "module",
  "files": [
    "dist",
    "openclaw.plugin.json",
    "README.md"
  ],
  "scripts": {
    "build": "esbuild index.ts --bundle --platform=node --format=esm --target=node22 --external:openclaw/* --outfile=dist/index.js"
  },
  "dependencies": {
    "typebox": "1.3.3"
  },
  "devDependencies": {
    "esbuild": "*"
  },
  "peerDependencies": {
    "openclaw": ">=2026.7.2"
  },
  "openclaw": {
    "extensions": [
      "./dist/index.js"
    ],
    "compat": {
      "pluginApi": ">=2026.7.2",
      "minGatewayVersion": "2026.7.2"
    },
    "build": {
      "openclawVersion": "2026.7.2",
      "pluginSdkVersion": "2026.7.2"
    }
  }
}
